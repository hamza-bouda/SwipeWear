# Secrets
.env
.env.local
.env*.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# IDE
.vscode/
.idea/

# Node
node_modules/
.next/

# OS
.DS_Store
Thumbs.db

# Local agent tooling — kept local except the shared agent team, whose
# definitions and plan must be versioned: a cloud routine clones the repo and
# would otherwise have no agents to delegate to.
.claude/*
!.claude/agents/
!.claude/commands/
!.claude/orchestration/

# Local runtime artefacts
backend/uvicorn.log
backend/.ingest_state.json

# Journaux de la CLI Firebase — bruit local, parfois verbeux sur les jetons.
firebase-debug.log
firestore-debug.log
ui-debug.log
